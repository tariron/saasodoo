http:
  routers:
    # Traefik Dashboard Router
    dashboard:
      rule: "Host(`traefik.62.171.153.219.nip.io`)"
      service: api@internal
      middlewares:
        - admin-auth
      entryPoints:
        - web

---
apiVersion: v1
kind: Service
metadata:
  name: kaui
  namespace: saasodoo
  labels:
    app.kubernetes.io/name: kaui
    app.kubernetes.io/component: admin-ui
    app.kubernetes.io/part-of: killbill
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: kaui
